# 线上后端依赖（EMBED_BACKEND=api）——不含 FlagEmbedding/torch，镜像小很多。
# 本地建索引仍用 backend/requirements.txt（含 FlagEmbedding）。
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
qdrant-client==1.11.3
numpy==2.1.0
python-dotenv==1.0.1
httpx==0.27.2
opencc-python-reimplemented==0.1.7
